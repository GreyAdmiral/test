// * Подключение активации кастомного селекта
import {choicesActivate} from "../functions/choices.js";

// * Подключение плейсхолдера
import {placeholderWatch} from "../functions/placeholder.js";

export const $functions = {
	choicesActivate,
	placeholderWatch,
};
